server:
  port: 9094

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/orderdb
      host: localhost
      port: 27017
      database: orderdb
      repositories:
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8717/eureka/